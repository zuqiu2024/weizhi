{
  "name": "visitor-collector",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "deploy": "node build.js && wrangler deploy",
    "dev": "node build.js && wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
